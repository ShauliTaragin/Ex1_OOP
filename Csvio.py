class Csvio:
    def __init__(self, df):
        self.df = df

    def __str__(self):
        return self.df


    def AllocateElevator(time , num_elev):
        return time

    #we need to create function (maybe using pandas) to parse df so we can create a call for elevator from each row in df
    #and function to change elev calls in df output